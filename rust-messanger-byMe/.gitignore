# ============================================
# Логи
# ============================================
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ============================================
# Зависимости и сборка фронтенда (Vite/Vue)
# ============================================
node_modules
dist
dist-ssr
*.local

# ============================================
# Базы данных SQLite (КРИТИЧНО!)
# ============================================
*.db
*.db-journal
*.db-shm
*.db-wal
messanger.db
messenger.db

# ============================================
# Файлы редакторов и IDE
# ============================================
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.iml

# ============================================
# Системные (Windows / macOS)
# ============================================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ============================================
# Переменные окружения и секреты (КРИТИЧНО!)
# ============================================
.env
.env.local
.env.*.local

# ============================================
# Tauri специфичное
# ============================================
# Билд-артефакты Tauri (уже покрывается src-tauri/target, но на всякий)
src-tauri/target
src-tauri/gen

# Файлы, созданные Tauri при упаковке приложения (bundles, msi, dmg, deb)
src-tauri/target/release/bundle/
src-tauri/target/debug/bundle/

# Ключи подписи приложений (НИКОГДА не коммитьте!)
*.p12
*.pfx
*.key
*.keystore
*.sign

# ============================================
# Rust / Cargo (дублирование с src-tauri/.gitignore)
# ============================================
Cargo.lock.bak